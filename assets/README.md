# Demo assets

Captured from a production build of the app running locally (`npm run build && npm run preview`).

- **`demo.gif`** — short loop cycling through the three core screens.
- **`home.png`** — gallery landing page (hero + "Why submit" highlights).
- **`upload.png`** — hand-drawn submission form with drag-and-drop upload.
- **`check.png`** — curator review dashboard (filters + upload).

![Demo loop](./demo.gif)
